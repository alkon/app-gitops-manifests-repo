# thanos-receiver/templates/servicemonitor.yaml
{{- if .Values.receiver.metrics.serviceMonitor.enabled }}
apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  name: {{ include "thanos-receiver-app.fullname" . }}
  namespace: {{ .Values.receiver.metrics.serviceMonitor.namespace }}
  labels:
    {{- include "thanos-receiver-app.labels" . | nindent 4 }}
    {{- with .Values.receiver.metrics.serviceMonitor.labels }}
    {{- toYaml . | nindent 4 }}
    {{- end }}
spec:
  selector:
    matchLabels:
      app.kubernetes.io/name: thanos
      app.kubernetes.io/instance: thanos-receiver
      app.kubernetes.io/component: thanos-receive
  endpoints:
  - port: http
    interval: 30s
    path: /metrics
{{- end }}